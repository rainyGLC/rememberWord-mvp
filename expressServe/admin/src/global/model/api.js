export default {
  classify: '/api/classify',
  classifyId: (id) => '/api/classify/' + id,
  word: '/api/word',
  wordId: (id) => '/api/word/' + id,
  upload: '/api/upload',
  uploadId: (id) => '/api/upload' + id
}
